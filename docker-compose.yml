version: "3.7"

services:
  app:
    image: ghcr.io/nateshoffner/lanco-bot:latest
    container_name: lanco-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
